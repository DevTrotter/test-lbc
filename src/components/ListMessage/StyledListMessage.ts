import styled from 'styled-components'

const StyledListMessage = styled.div`
  padding: 24px 24px;
  min-height: calc(100vh - 200px);
  max-height: calc(100vh - 200px);
  overflow: auto;
`

export default StyledListMessage